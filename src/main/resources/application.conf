AffirmationsConfig {
    api {
      port = 8080
      port = ${?PORT}
    }

    db {
      url = "jdbc:postgresql://0.0.0.0:5432/postgres"
      url = ${?POSTGRES_URL}
      user = "postgres"
      user = ${?POSTGRES_USER}
      password = "password"
      password = ${?POSTGRES_PASSWORD}
    }
}