AffirmationsConfig {
    api {
      port = 8080
      port = ${?PORT}
    }

    db {
      url = "jdbc:postgresql:default_database"
      user = "postgres"
      user = ${?POSTGRES_USER}
      password = "password"
      password = ${?POSTGRES_PASSWORD}
    }
}